[workspace]
release_always = false
git_release_body = """
{{ changelog }}
{% if remote.contributors %}
### Thanks to the contributors for this release:
{% for contributor in remote.contributors %}
* @{{ contributor.username }}
{% endfor %}
{% endif %}
"""

[[package]]
name = "ext-php-rs"
publish_features = ["enum", "runtime", "closure", "embed", "anyhow"]
# Off until 0.16.0 is the published baseline. cargo-semver-checks builds the
# baseline from its own manifest, and 0.15.15 names the feature `static`, which
# clang-sys refuses to combine with `runtime`. 0.16.0 renames it `_static`, a
# name cargo-semver-checks skips, so this can go back to the default then.
semver_check = false

# Off until 0.2.0 is the published baseline. cargo-semver-checks builds the
# baseline from its own manifest, and 0.1.21 forwards `runtime` and `static` to
# `ext-php-rs`, a pair clang-sys refuses to combine. `crates/cli` declares no
# feature at all since it stopped depending on `ext-php-rs`, so this can go back
# to the default once 0.2.0 is out.
[[package]]
name = "cargo-php"
semver_check = false

[changelog]
header = "# Changelog"
body = """
{%- macro username(commit) -%}
  {% if commit.remote.username %} (by @{{ commit.remote.username }}){% endif -%}
{% endmacro -%}
{% macro commit_message(commit) %}
  - {% if commit.scope %}*({{ commit.scope }})* {% endif %}\
    {% if commit.breaking %}[**breaking**] {% endif %}\
    {{ commit.message | upper_first }}\
    {{ self::username(commit=commit) }} \
    {%- if commit.links %} \
      {% for link in commit.links | unique(attribute="href") %}\
        [[{{link.text}}]({{link.href}})] \
      {% endfor %}\
    {% endif %}
  {%- if commit.breaking and commit.breaking_description and not commit.breaking_description == commit.message%}
    > {{ commit.breaking_description -}}
  {% endif -%}
{% endmacro %}
## [{{ version | trim_start_matches(pat="v") }}]{%- if release_link -%}({{ release_link }}){% endif %} - {{ timestamp | date(format="%Y-%m-%d") -}}
{%- set breaking_commits = commits | filter(attribute="breaking", value=true) %}
{%- if breaking_commits | length > 0 %}

  ### BREAKING CHANGES
  {% for commit in breaking_commits -%}
    {{- self::commit_message(commit=commit) -}}
  {% endfor -%}
{% endif %}
{%- for group, commits in commits | filter(attribute="breaking", value=false) | group_by(attribute="group") %}

  ### {{ group | upper_first -}}
  {% for commit in commits
  | filter(attribute="scope")
  | sort(attribute="scope") -%}
    {{- self::commit_message(commit=commit) -}}
  {% endfor -%}
  {%- for commit in commits -%}
    {% if not commit.scope -%}
      {{- self::commit_message(commit=commit) -}}
    {% endif -%}
  {% endfor -%}
{% endfor %}
"""
link_parsers = [
  { pattern = "#(\\d+)", href = "https://github.com/extphprs/ext-php-rs/issues/$1" },
]
