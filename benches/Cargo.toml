[package]
name = "benches"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
divan = { package = "codspeed-divan-compat", version = "5.0.1" }

[features]
default = ["enum", "runtime", "closure"]
enum = []
anyhow = []
runtime = []
closure = []
static = []

[[bench]]
name = "binary_bench"
harness = false

[workspace]
